// post should be able to have pictures videos and the like, we will store it in our database to always be accesable 
//post will have a user section, a description section, and a comments section, aswell as likes,

//post pictures will be downloaded to server, stored, and path will be accessable to post schema,


const {schema, model} = require("mongoose");

const postSchema = new Schema({
  post: {
    type: 
  }
})